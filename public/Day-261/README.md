
  # Create design elements

  This is a code bundle for Create design elements. The original project is available at https://www.figma.com/design/aAhaplxhhDOQ9SObU5CUc8/Create-design-elements.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  